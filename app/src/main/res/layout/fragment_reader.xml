<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/black">
    <!-- Grey background loading screen commented out - using fade transitions instead
    <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/progressPlaceholder"
            android:visibility="visible">
        <include layout="@layout/progress_image"/>
    </RelativeLayout>
    -->
    <com.nkanaev.comics.view.ComicViewPager
            android:id="@+id/viewPager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:visibility="invisible"/>
    <!-- Black overlay that fades out when content loads -->
    <View
            android:id="@+id/blackOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/black"
            android:visibility="visible"/>
</RelativeLayout>